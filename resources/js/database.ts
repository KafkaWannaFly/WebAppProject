import { Item } from "./Models/item";
import { User } from "../js/Models/user";

// Get items in database
function getItems(amount: number): Item[] {
	return null;
}

// Get item by ID
function getItem(id: string): Item {
	return null;
}

function checkAccount(username: string, password: string): User {
	return null;
}

function createAccount(user: User): boolean {
	return false;
}

function forgetPassword(username: string, email: string): string {
	return;
}
